# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# build output & test artifacts
dist
*.png

# source spreadsheets / pdfs
*.xlsx
*.pdf
~$*

# NOTE: public/icons/ is deliberately NOT ignored. src/data/icons.json names an
# exact file for all 384 entries, and the app renders an <img> for every one of
# them, so a build without the artwork present shows broken images.
